import RPi.GPIO as GPIO
from time import sleep
GPIO.setode(GPIO.BCM)
GPIO.setwarnings(false)
Pins = [10,9,18,14,15,17,23,24,24,12,16,20,21]
for i Pins:
	GPIO.seup(i,GPIO.OUT)
pwm = GPIO.PWM(18,100)
pwm1 = GPIO.PWM(17,100)
pwm2 = GPIO.OUT()
